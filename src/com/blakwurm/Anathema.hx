package com.blakwurm;

import tink.Cli;
import com.blakwurm.djala.System;

class Anathema {
    public static function main() {
        trace("Starting thing!");
        Cli.process(Sys.args(), new SystemArgs());
        trace("Done Starting Thing!");
    }
}